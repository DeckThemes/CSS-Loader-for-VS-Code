{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Theme",
  "description": "The config file for your CSS Loader Theme.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The JSON schema for a CSS Loader Theme.",
      "type": [
        "string",
        "null"
      ]
    }
  }
}